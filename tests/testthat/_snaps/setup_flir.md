# setup_flir works for packages

    Code
      fs::dir_tree("flir")
    Output
      flir
      +-- cache_file_state.rds
      \-- config.yml

# setup_flir works for projects

    Code
      fs::dir_tree("flir")
    Output
      flir
      +-- cache_file_state.rds
      \-- config.yml

